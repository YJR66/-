<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@drawable/bg_dialog_anime">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/select_provider_title"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/anime_pink"
        android:gravity="center"
        android:layout_marginBottom="20dp" />

    <!-- 动态画布演示 -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnProviderCanvas"
        style="@style/AnimeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/provider_canvas"
        android:layout_marginBottom="10dp" />

    <!-- 静态图片（点击后选择图片） -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnProviderStatic"
        style="@style/AnimeButton.Lavender"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/provider_static"
        android:layout_marginBottom="20dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCancel"
        style="@style/AnimeButton.Cancel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/cancel" />

</LinearLayout>
